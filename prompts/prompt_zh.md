# 任务目标
你需要给出一段python元组或者生成元组的python代码，确保用户拿到的数据可以用tv.py在终端渲染出高质量的动态/静态图像（动态图像就是视频，就是把多帧数据放一个元组里）

# 核心渲染规则（必须严格遵守）
你生成的每一个字符，都必须同时包含两个独立的垂直像素信息。请严格遵循以下物理映射规则：

1. **字符与物理网格**
   - 你的渲染网格宽度为 `width`，高度为 `height`（这里的 `height` 是字符行数）。
   - 终端中每一行字符，实际上代表了**两行物理像素**。
   - 物理像素的总高度为 `height * 2`。

2. **坐标与颜色映射（最关键）**
   - 字符所在的列记为 `x`（范围 0 到 `width-1`）。
   - 字符所在的行记为 `row`（范围 0 到 `height-1`）。
   - 对于当前字符 `(x, row)`：
     - **上半块物理坐标**：`(x, y_top = row * 2)`。此处的颜色必须映射到 **ANSI 背景色** (`\033[48;2;R;G;Bm`)。
     - **下半块物理坐标**：`(x, y_bot = row * 2 + 1)`。此处的颜色必须映射到 **ANSI 前景色** (`\033[38;2;R;G;Bm`)。
   - **你必须为每一个字符独立计算这两个像素的颜色，绝不能共享整行或整列的计算结果。**

3. **消除色带/横条纹的约束（必须强制执行）**
   - 因为 `y_top` 和 `y_bot` 在物理上仅仅垂直相差 1 个像素，所以它们渲染出的颜色通常非常接近。
   - **绝对禁止**为了“省事”而将背景色强制涂黑（`0,0,0`）或涂成固定色。
   - 上下两半块的颜色必须严格遵守物理图像中这两个相邻坐标点的实际颜色，以确保上下行字符拼接时，画面是**垂直方向完全连续、无缝衔接**的，从而彻底杜绝“斑马线”或“横向断层”效果。

4. **目标效果**
   - 最终屏幕上的画面，应该是一幅完整且连续的高分辨率图像，由无数个“上半块”和“下半块”拼合而成，画面左右、上下均平滑过渡。

5. **颜色采样函数要求**
   - 你需要编写一个单独的函数或算法，例如 `get_color(x, y)`，该函数接受物理坐标 `x` 和 `y`，返回该坐标点处的 `(R, G, B)` 颜色值。
   - 在生成字符循环中，分别传入 `y_top` 和 `y_bot` 来获取对应的颜色。

# 其他要求
- 需要确保 ANSI 转义字符串被正确格式化。
- 你的任务是生成能直接给出元组或者生成元组的代码